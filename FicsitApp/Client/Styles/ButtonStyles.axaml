<Styles xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Design.PreviewWith>
        <Border Padding="20">
            <Button Classes="List" Margin="5 3">
                <PathIcon Data="{StaticResource EditRegular}"></PathIcon>
            </Button>
        </Border>
    </Design.PreviewWith>

    <!-- Navigation Button -->
    <Style Selector="Button.NavBtn">
        <Setter Property="Width" Value="100" />
        <Setter Property="Height" Value="100" />
        <Setter Property="BorderBrush" Value="Black" />
        <Setter Property="BorderThickness" Value="2" />
        <Setter Property="CornerRadius" Value="5" />
        <Setter Property="Margin" Value="0 0 0 3" />
    </Style>
    
    <Style Selector="Button.NavBtn:pointerover /template/ ContentPresenter">
        <Setter Property="Background" Value="{DynamicResource BrushStandardOrange}" />
        <Setter Property="BorderBrush" Value="Black" />
    </Style>
    
    <!-- Dialog Button -->
    <Style Selector="Button.Dialog">
        <Setter Property="FontFamily" Value="{DynamicResource Poppins}" />
        <Setter Property="FontSize" Value="15" />
        <Setter Property="Padding" Value="8 3" />
        <Setter Property="BorderBrush" Value="Black" />
    </Style>
    
    <Style Selector="Button.Dialog:pointerover /template/ ContentPresenter">
        <Setter Property="Background" Value="{DynamicResource BrushStandardOrange}" />
        <Setter Property="BorderBrush" Value="Black" />
    </Style>
    
    <!-- PathIcon Buttons -->
    <Style Selector="Button.PathIcon:pointerover /template/ ContentPresenter">
        <Setter Property="Background" Value="{DynamicResource BrushStandardOrange}" />
    </Style>
    
    <!--  ListItem Buttons -->
    <Style Selector="Button.List">
        <Setter Property="Background" Value="{DynamicResource BrushBackground}" />
    </Style>
    
    <Style Selector="Button.List:pointerover /template/ ContentPresenter">
        <Setter Property="Background" Value="DimGray" />
    </Style>
    
    <Style Selector="Button.List PathIcon">
        <Setter Property="Foreground" Value="{DynamicResource BrushStandardOrange}" />
    </Style>
    
</Styles>
